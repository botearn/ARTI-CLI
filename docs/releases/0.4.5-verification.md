# ARTI CLI 0.4.5 Release Verification

## Scope

- Candidate version: `0.4.5`
- Branch: `release/0.4.5`
- Environment: local packaging plus read-only Dev replay
- Production touched: no
- Database or Migration touched: no
- New report task or credit charge: no
- NPM publication: pending explicit approval

## Local Gates

| Gate | Result |
|---|---|
| `npm test` | Passed: 43 files, 185 tests |
| `npx tsc --noEmit` | Passed |
| `npm run build` | Passed |
| `git diff --check` | Passed |
| Candidate version | `artifin-cli@0.4.5` |

## Package Inspection

The candidate tarball was built with `npm pack` and installed into a new
temporary project with an empty `HOME`.

| Field | Result |
|---|---|
| Filename | `artifin-cli-0.4.5.tgz` |
| Packed size | 67.5 kB |
| Unpacked size | 253.7 kB |
| File count | 4 |
| Files | `README.md`, `dist/index.js`, `dist/index.d.ts`, `package.json` |
| Clean-install version | `0.4.5` |
| Default Harness gate | Passed: exits before auth/network and prints the opt-in command |

The package contains no source tree, tests, CSV ledger, Migration, deployment
configuration, credentials, or local session files.

## Read-only Dev Replay

| Scenario | Run ID | Roles | Evidence | Cards | Decision | Gate |
|---|---|---:|---:|---:|---|---|
| MSFT Panorama | `b20be658-a004-4b49-a6e5-d63208205fda` | 5 | 40 | 8 | `pass_with_gaps` | Passed |
| MSFT resume after 50 | same | 2/2 in replay window | 3 in replay window | n/a | `pass_with_gaps` | Passed |
| NVDA Deep | `0f7631d3-5ad8-4511-8529-927da5b39d57` | 9 | 113 | 9 | `pass_with_gaps` | Passed |

MSFT resume returned only sequences 51 through 55 and labeled all role,
evidence, and duration values as replay-window statistics.

## Residual Risk

`npm audit` reports two Moderate findings from the Node.js 18-compatible
`@hono/node-server` 1.x dependency and one Low development-only `esbuild`
finding. ARTI CLI uses the MCP client transport and does not start the affected
Windows static-file server. The High dependency finding present on the previous
lockfile is removed.

## Release Gate

Do not merge the version PR until a human explicitly approves publishing
`artifin-cli@0.4.5`. Merging the PR into `main` invokes the NPM publish workflow.
